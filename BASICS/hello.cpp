#include <iostream> //tells the compiler to include the iostream library, which allows us to use input and output functions like std::cout

int main() {//the main function is the entry point of the program, where execution starts
    std::cout << "Hello Siphokazi! Your C++ setup is working correctly." << std::endl; //std::cout is used to print the message to the console, and *std::endl* adds a new line after the message
    std::cout <<"Hello, my name is Siphokazi Mdze and I am learning C++ programming language." << std::endl; //prints another message to the console
    return 0;//return 0 indicates that the program ended successfully
}